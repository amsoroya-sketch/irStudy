# PRD: Backend Testing Implementation (P1-005 Integration + Security Tests)

**PRD ID**: PRD-P1-009-BACKEND-TESTING-IMPLEMENTATION
**Category**: Backend Testing (Pytest, Performance, Security)
**Priority**: P1-Critical (Validates Backend Functionality + Security)
**Estimated Effort**: 8-10 hours
**Dependencies**: PRD-P1-005 (Auto Study Card Generation complete)
**Status**: Ready for Implementation
**Assigned Agents**: `python-backend-developer`, `testing-qa-expert`, `security-compliance-expert`

**Version**: 1.0
**Created**: 2026-03-24
**Last Updated**: 2026-03-24

---

## R - REQUEST (What & Why)

### Executive Summary

Implement **27 comprehensive backend tests** covering study cards pipeline integration (P1-005 ↔ P1-006 ↔ P1-007), security validation, and performance benchmarks. This PRD ensures production-ready backend with zero-tolerance security enforcement, <8s card generation performance, and 100% test pass rate.

**Test Breakdown**:
- **15 Integration Tests**: P1-005→P1-006 (5), P1-006↔P1-007 (5), Full Pipeline (5)
- **5 Security Tests**: XSS prevention, SQL injection, authorization, JWT expiration, rate limiting
- **7 Performance Tests**: <8s generation, <200ms API, <100ms SM-2 update, 50 concurrent users

**Business Impact**:
- **Zero security vulnerabilities**: XSS, SQL injection, auth bypass prevented
- **Performance validated**: <8s card generation guaranteed (not estimated)
- **Regression prevention**: Catch backend bugs before deployment
- **SM-2 consistency**: Python backend matches TypeScript frontend (within 0.01)
- **Production confidence**: Automated tests faster than manual QA (5 min vs. 45 min)

**Success Criteria**:
- ✅ 27/27 tests pass (100% pass rate)
- ✅ ≥80% line coverage, ≥85% function coverage
- ✅ 0 security violations (bandit scan clean)
- ✅ <8s card generation (3-5 cards)
- ✅ <200ms review API response
- ✅ 50 concurrent users supported

---

## A - ARCHITECTURE

### Testing Stack

**Unit Tests** (Pytest + Pytest-asyncio):
- **Framework**: Pytest 7.4+
- **Async Support**: pytest-asyncio 0.23+
- **HTTP Testing**: HTTPX AsyncClient
- **Database**: SQLite in-memory (fast) or PostgreSQL test database
- **Mocking**: pytest.monkeypatch, unittest.mock

**Integration Tests** (Pytest + TestClient):
- **API Testing**: FastAPI TestClient (synchronous) or HTTPX AsyncClient
- **Database**: PostgreSQL test database with transactions
- **External Services**: Mock Anthropic API, Qdrant (RAG)

**Security Tests** (Pytest + Bandit):
- **Penetration Testing**: XSS payloads, SQL injection attempts
- **Authorization**: User isolation, JWT validation
- **Secrets Scanning**: Bandit static analysis

**Performance Tests** (Pytest + time.time()):
- **Benchmarks**: Card generation, API response times
- **Load Testing**: 50 concurrent users with asyncio.gather()
- **Profiling**: cProfile for bottleneck detection

### File Structure

```
backend/
├── src/
│   ├── ai/
│   │   ├── study_card_generator.py          # Already implemented (P1-005)
│   │   └── rag_service.py                   # Already implemented (P1-005)
│   ├── api/
│   │   └── v1/
│   │       └── study_cards.py               # Already implemented (P1-005)
│   └── schemas/
│       └── study_card.py                    # Already implemented (P1-005)
├── tests/
│   ├── conftest.py                          # Shared fixtures
│   ├── test_integration/
│   │   ├── __init__.py
│   │   ├── test_study_cards_pipeline.py     # 5 tests (P1-005→P1-006)
│   │   ├── test_review_to_sm2.py            # 5 tests (P1-006↔P1-007)
│   │   └── test_full_pipeline.py            # 5 tests (Complete workflow)
│   ├── test_security/
│   │   ├── __init__.py
│   │   └── test_study_cards_security.py     # 5 security tests
│   └── test_performance/
│       ├── __init__.py
│       └── test_study_cards_performance.py  # 7 performance tests
├── pytest.ini                               # Pytest configuration
└── requirements-test.txt                    # Test dependencies
```

---

## L - LOOP (Iterative Development Phases)

### Phase 1: Pytest Setup + P1-005→P1-006 Integration (2-3 hours)

**Deliverables**:
- Configure pytest with coverage thresholds (80% line, 85% function)
- Create conftest.py with shared fixtures (db, async_client, test_user)
- Implement `test_study_cards_pipeline.py` (5 tests):
  1. Cards generated by P1-005 fetchable by P1-006 API
  2. Citations from P1-005 display correctly in P1-006
  3. SM-2 parameters initialized correctly
  4. No placeholder content in review interface
  5. Idempotency prevents duplicate cards

**Validation**:
```bash
pytest tests/test_integration/test_study_cards_pipeline.py -v
# Expected: 5/5 tests PASSED
```

---

### Phase 2: P1-006↔P1-007 Integration Tests (2-3 hours)

**Deliverables**:
- Implement `test_review_to_sm2.py` (5 tests):
  1. Rating card triggers SM-2 calculation
  2. TypeScript-Python SM-2 consistency (10 test cases)
  3. Optimistic UI update matches backend result
  4. Next review date filter works after rating
  5. Concurrent ratings no race condition

**Validation**:
```bash
pytest tests/test_integration/test_review_to_sm2.py -v
# Expected: 5/5 tests PASSED
```

---

### Phase 3: Full Pipeline Integration Tests (2 hours)

**Deliverables**:
- Implement `test_full_pipeline.py` (5 tests):
  1. Complete workflow (OSCE → generate → review → rate)
  2. Performance (<15 seconds for full pipeline)
  3. Data consistency across layers
  4. Error isolation (P1-005 failure doesn't break P1-006)
  5. Atomic operations (rollback on partial failure)

**Validation**:
```bash
pytest tests/test_integration/test_full_pipeline.py -v
# Expected: 5/5 tests PASSED
```

---

### Phase 4: Security Tests (2-3 hours)

**Deliverables**:
- Implement `test_study_cards_security.py` (5 tests):
  1. XSS prevention in question/answer fields
  2. SQL injection prevention in session_id parameter
  3. Authorization (users can only access their own cards)
  4. JWT token expiration handling
  5. Rate limiting (prevent abuse)
- Run bandit security scan

**Validation**:
```bash
pytest tests/test_security/ -v
bandit -r src/
# Expected: 5/5 tests PASSED, 0 high-severity issues
```

---

### Phase 5: Performance Tests (2 hours)

**Deliverables**:
- Implement `test_study_cards_performance.py` (7 tests):
  1. Card generation <8 seconds
  2. Review API <200ms
  3. SM-2 update <100ms
  4. Concurrent load (50 users)
  5. Database query performance
  6. RAG query performance
  7. Memory usage profiling

**Validation**:
```bash
pytest tests/test_performance/ -v -m performance
# Expected: 7/7 tests PASSED
```

---

### Phase 6: Final QA + Coverage Report (1-2 hours)

**Deliverables**:
- Run full test suite with coverage
- Generate coverage HTML report
- Fix any failing tests
- Verify coverage ≥80% (lines), ≥85% (functions)
- Document performance benchmarks

**Validation**:
```bash
pytest tests/ --cov=src --cov-report=html --cov-report=term-missing
# Expected: 27/27 tests PASSED, coverage ≥80%
```

---

## P - PLAN (Detailed Implementation)

### Phase 1: Pytest Setup + P1-005→P1-006 Integration

#### Step 1.1: Install Test Dependencies

```bash
cd /home/dev/Development/irStudy/backend
pip install -r requirements-test.txt
```

**File**: `backend/requirements-test.txt`

```
pytest==7.4.4
pytest-cov==4.1.0
pytest-asyncio==0.23.3
httpx==0.25.2
faker==20.1.0
```

#### Step 1.2: Create Pytest Configuration

**File**: `backend/pytest.ini`

```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
    --cov=src
    --cov-report=term-missing
    --cov-report=html
    --cov-report=xml
    --cov-fail-under=80
    -p no:warnings
markers =
    unit: Unit tests
    integration: Integration tests
    security: Security tests
    performance: Performance benchmarks
asyncio_mode = auto
```

#### Step 1.3: Create Shared Test Fixtures

**File**: `backend/tests/conftest.py`

```python
import pytest
from httpx import AsyncClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, Session
from datetime import datetime, timedelta
import jwt

from src.main import app
from src.db.base import Base, get_db
from src.db.models import User, OSCEAttemptAI, StudyCard


# Test database URL (in-memory SQLite for speed)
TEST_DATABASE_URL = "sqlite:///./test.db"


@pytest.fixture(scope="function")
def db() -> Session:
    """Create test database session"""
    engine = create_engine(TEST_DATABASE_URL, connect_args={"check_same_thread": False})
    Base.metadata.create_all(bind=engine)
    TestingSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

    db = TestingSessionLocal()
    try:
        yield db
    finally:
        db.close()
        Base.metadata.drop_all(bind=engine)


@pytest.fixture(scope="function")
async def async_client(db: Session):
    """Create async HTTP client with test database"""
    def override_get_db():
        try:
            yield db
        finally:
            pass

    app.dependency_overrides[get_db] = override_get_db

    async with AsyncClient(app=app, base_url="http://test") as client:
        yield client

    app.dependency_overrides.clear()


@pytest.fixture(scope="function")
def test_user(db: Session) -> User:
    """Create test user"""
    user = User(
        email="test@example.com",
        role="student",
        hashed_password="hashed_password_here",
        is_active=True,
        created_at=datetime.utcnow(),
    )
    db.add(user)
    db.commit()
    db.refresh(user)
    return user


def generate_jwt_token(user: User) -> str:
    """Generate JWT token for test user"""
    payload = {
        "user_id": user.user_id,
        "email": user.email,
        "role": user.role,
        "exp": datetime.utcnow() + timedelta(hours=1),
        "iat": datetime.utcnow(),
        "type": "access",
    }
    return jwt.encode(payload, "test_secret_key", algorithm="HS256")


# Make generate_jwt_token available to all tests
@pytest.fixture
def jwt_token_generator():
    return generate_jwt_token
```

#### Step 1.4: Implement test_study_cards_pipeline.py

(Full implementation provided in COMPREHENSIVE-TESTING-PLAN-STUDY-CARDS-PART2.md, tests 86-90)

---

## H - HANDOFF (Acceptance Criteria & Testing)

### Acceptance Criteria

**MUST HAVE** (100% Required):
- [ ] **27/27 tests pass** (100% pass rate, zero tolerance)
- [ ] **Coverage ≥80%** (lines), ≥85% (functions)
- [ ] **0 security violations** (bandit scan clean)
- [ ] **Performance benchmarks met** (<8s generation, <200ms API, <100ms SM-2)
- [ ] **SM-2 consistency** (Python matches TypeScript within 0.01)
- [ ] **50 concurrent users** supported without errors
- [ ] **0 XSS vulnerabilities** (all payloads escaped)
- [ ] **0 SQL injection** (parameterized queries only)
- [ ] **JWT required** (unauthorized requests rejected with 401)
- [ ] **Rate limiting enforced** (429 returned after threshold)

**SHOULD HAVE** (90% Priority):
- [ ] **Test execution time** <5 minutes for full suite
- [ ] **Coverage report** generated as HTML
- [ ] **Performance profiling** (cProfile for bottleneck detection)

**NICE TO HAVE** (Optional):
- [ ] **Load testing** with Locust (1000+ concurrent users)
- [ ] **Database stress testing** (10,000+ cards)
- [ ] **Memory profiling** (memory_profiler)

---

### 3-Layer QA Validation (Phase 6 - MANDATORY FINAL APPROVAL)

#### Layer 1: Agent Self-Validation

**Agent MUST run these commands and confirm 0 errors:**

```bash
cd /home/dev/Development/irStudy/backend

# 1. Run all tests
source venv/bin/activate
export DATABASE_PASSWORD="MUVkFS6TlWR2IhYm6VTqXXMW2Nz+EkkARbdu/s1dYBs="
pytest tests/ -v
# Expected: ✅ 27/27 tests PASSED

# 2. Generate coverage report
pytest tests/ --cov=src --cov-report=html --cov-report=term-missing
# Expected: ✅ Coverage ≥80% (lines), ≥85% (functions)

# 3. Run security scan
bandit -r src/ -f json -o bandit-report.json
# Expected: ✅ 0 high-severity issues

# 4. Run performance tests
pytest tests/test_performance/ -v -m performance
# Expected: ✅ All performance benchmarks met

# 5. Check for hardcoded secrets
grep -r "sk-ant-\|api_key.*=" src/ || echo "✅ No hardcoded secrets"
# Expected: ✅ No matches

# 6. Validate SM-2 consistency
pytest tests/test_integration/test_review_to_sm2.py::test_typescript_python_sm2_consistency -v
# Expected: ✅ All 10 test cases match within 0.01 tolerance
```

**Agent Checklist**:
- [ ] 27/27 tests PASSED (no flaky tests)
- [ ] Coverage ≥80% lines, ≥85% functions
- [ ] Bandit scan 0 high-severity issues
- [ ] Performance benchmarks met (<8s, <200ms, <100ms)
- [ ] SM-2 consistency validated (10/10 cases within 0.01)
- [ ] 0 hardcoded secrets
- [ ] Security tests pass (XSS, SQL injection prevented)

---

#### Layer 2: PM Independent Verification

**PM runs SAME commands:**

```bash
cd /home/dev/Development/irStudy/backend
source venv/bin/activate
export DATABASE_PASSWORD="MUVkFS6TlWR2IhYm6VTqXXMW2Nz+EkkARbdu/s1dYBs="

# 1. Verify tests pass
pytest tests/ -v
# Expected: ✅ 27/27 PASSED

# 2. Verify coverage (visual inspection)
pytest tests/ --cov=src --cov-report=html
open htmlcov/index.html  # Check coverage visually
# Expected: ✅ study_card_generator.py ≥80%, study_cards.py ≥80%

# 3. Spot-check critical tests
pytest tests/test_security/test_study_cards_security.py::test_xss_prevention_in_card_content -v
pytest tests/test_performance/test_study_cards_performance.py::test_card_generation_under_8_seconds -v
# Expected: ✅ Both pass
```

**PM Checklist**:
- [ ] Tests verified (PM saw 27/27 PASSED)
- [ ] Coverage verified (HTML report shows ≥80%)
- [ ] Security tests spot-checked (XSS, SQL injection prevented)
- [ ] Performance tests spot-checked (<8s generation verified)
- [ ] Code quality reviewed (test structure, error handling)

---

#### Layer 3: testing-qa-expert + security-compliance-expert Review

**QA expert runs comprehensive validation:**

```bash
cd /home/dev/Development/irStudy/backend
source venv/bin/activate
export DATABASE_PASSWORD="MUVkFS6TlWR2IhYm6VTqXXMW2Nz+EkkARbdu/s1dYBs="

# 1. Full test suite (stress test - run 3 times)
for i in {1..3}; do
  pytest tests/ -v
done
# Expected: ✅ 100% pass rate (no flaky failures)

# 2. Deep coverage analysis
pytest tests/ --cov=src --cov-report=html --cov-branch
# Check specific files:
# - study_card_generator.py: ≥85% line, ≥80% branch
# - study_cards.py: ≥85% line, ≥80% branch
# - rag_service.py: ≥80% line

# 3. Security deep scan
bandit -r src/ -ll -f screen
# Expected: ✅ 0 high/medium severity issues

# 4. Performance stress test (50 concurrent users)
pytest tests/test_performance/test_study_cards_performance.py::test_concurrent_load_50_users -v
# Expected: ✅ All 50 requests succeed in <5s

# 5. SM-2 consistency (critical validation)
pytest tests/test_integration/test_review_to_sm2.py::test_typescript_python_sm2_consistency -v --tb=short
# Expected: ✅ All 10 cases within 0.01 tolerance

# 6. SQL injection test (penetration testing)
pytest tests/test_security/test_study_cards_security.py::test_sql_injection_prevention -v
# Expected: ✅ All payloads rejected

# 7. Authorization test (critical)
pytest tests/test_security/test_study_cards_security.py::test_authorization_prevents_card_access_across_users -v
# Expected: ✅ Users isolated (no data leakage)
```

**QA Checklist**:
- [ ] 100% test pass rate (ran 3 times, 0 flaky failures)
- [ ] Coverage ≥80% verified (HTML report, branch coverage checked)
- [ ] Security scan clean (0 high/medium issues)
- [ ] Performance benchmarks met (<8s, <200ms, <100ms, 50 users)
- [ ] SM-2 consistency validated (10/10 cases)
- [ ] SQL injection prevented (all payloads rejected)
- [ ] Authorization enforced (user isolation validated)
- [ ] XSS prevented (script tags escaped)

**QA Decision**: ✅ APPROVE Phase 6 / ❌ REJECT Phase 6

---

### Final Approval Signature

- [ ] PM Sign-Off: _______________ Date: _______
- [ ] testing-qa-expert Sign-Off: _______________ Date: _______
- [ ] security-compliance-expert Sign-Off: _______________ Date: _______

**PRD-P1-009 Status**: ⏳ INCOMPLETE / ✅ COMPLETE

---

## Validation Commands (Quick Reference)

```bash
# All tests
pytest tests/ -v

# Coverage report
pytest tests/ --cov=src --cov-report=html

# Integration tests only
pytest tests/test_integration/ -v

# Security tests only
pytest tests/test_security/ -v

# Performance tests only
pytest tests/test_performance/ -v -m performance

# Specific test file
pytest tests/test_integration/test_study_cards_pipeline.py -v

# Specific test function
pytest tests/test_security/test_study_cards_security.py::test_xss_prevention_in_card_content -v

# Security scan
bandit -r src/

# With verbose output
pytest tests/ -v --tb=long

# Fast failure (stop on first error)
pytest tests/ -x
```

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
